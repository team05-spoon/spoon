<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spoon.dao.memberDAO">

    <select id="listDao" resultType="com.spoon.dto.memberDTO">
		select * from member order by m_no asc
	</select>
	<select id="viewDao" resultType="com.spoon.dto.memberDTO">
		select * from member where m_no = #{param1}
	</select>
	<select id="findByMid" resultType="com.spoon.dto.memberDTO">
    SELECT * from member where m_id = #{m_id}
	</select>
	<select id="findByMno" resultType="com.spoon.dto.memberDTO">
    SELECT * from member where m_no = #{m_no}
	</select>
	<insert id="writeDao">
		insert into member values (seq_member.nextval,#{m_id},#{m_pw},#{m_name},#{m_tel},#{m_email},#{m_addr},#{m_birth},'USER')
	</insert>
	<update id="updateDao">
		update member set m_pw=#{m_pw},m_tel=#{m_tel},m_email=#{m_email},m_addr=#{m_addr} where m_no=#{m_no}
	</update>
	<delete id="deleteDao">
		delete from member where m_no=#{param1} 
	</delete>

</mapper>

